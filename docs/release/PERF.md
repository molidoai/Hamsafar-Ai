# بهینه‌سازی سرور محلی

- کش فایل وب و ادمین در حافظه
- کش مانیفست آپدیت ۳۰ ثانیه
- سقف بدن درخواست ۶۴ کیلوبایت
- HTTP keep-alive
- فقط استماع روی 127.0.0.1

بعد از `git pull` دوباره `npx --yes tsx src/server.ts`
